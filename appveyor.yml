-
  branches:
    only:
      - dev
      
  init:
    - git config --global core.autocrlf input
  build_script:
    - cmd: build.cmd
  test: off
  version: 0.3.{build}
  artifacts:
    - path: bin
      name: bin
