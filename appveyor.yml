-
  branches:
    only:
      - dev
      
  init:
    - git config --global core.autocrlf input
  install:
    -cmd: git submodule update --init --recursive
  build_script:
    - cmd: build.cmd
  test: off
  version: 0.3.{build}
  artifacts:
    - path: bin
      name: bin
